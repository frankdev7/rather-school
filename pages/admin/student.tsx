import Head from 'next/head'
import { GetStaticProps, GetStaticPropsResult } from 'next'
import RoomsTable from '../components/table/room/RoomTable'
import StudentsTable from '../components/table/student-room/StudentsTable'
import { createTheme, ThemeProvider, } from '@mui/material/styles'
import { ratherThemeOptions } from '../../ratherThemeOptions'
import NavbarAdmin from '../components/navbar/NavbarAdmin'
import DataTable from '../components/table/room/RoomTable'
import StudentTable from '../components/table/student/StudentTable'

const theme = createTheme(ratherThemeOptions);

interface Props {
}

export default function Student() {
  return (
    <ThemeProvider theme={theme}>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <NavbarAdmin />
      <main>
        <StudentTable />
      </main>
    </ThemeProvider >
  )
}

export const getStaticProps: GetStaticProps<Props> = async (): Promise<GetStaticPropsResult<Props>> => {
  // const rooms = await getRooms();
  return {
    props: {
    }
  };
}
